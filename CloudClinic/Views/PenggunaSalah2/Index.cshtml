@{
    ViewBag.Title = "Setup Dokter";
}

<h2>Setup Dokter</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>


@(Html.Kendo().Grid<CloudClinic.Models.ViewModel.UserViewModel>().Name("grid").Columns(columns =>
{
    columns.Bound(p => p.PenggunaId).Width(50);
    columns.Bound(p => p.UserName).Width(80);
    //columns.Bound(p => p.Password).Width(80);
    columns.Bound(p => p.Aturan).Width(100);
    columns.Bound(p => p.Nama).Width(100);
    columns.Bound(p => p.Kota).Width(100);
    columns.Bound(p => p.Alamat).Width(120);
    columns.Bound(p => p.Telp).Width(100);
    columns.Bound(p => p.Email).Width(100);
    columns.Command(command => { command.Edit(); command.Destroy(); }).Width(160);
})
        .ToolBar(toolbar => toolbar.Create())
        .Editable(editable => editable.Mode(GridEditMode.PopUp))
        .Pageable()
        .Sortable()
        .Scrollable()
        .HtmlAttributes(new { style = "height:430px;" })
            .DataSource(dataSource => dataSource
            .Ajax()
            .PageSize(20)
            .Events(events => events.Error("error_handler"))
                .Model(model => model.Id(p => p.PenggunaId))
                .Create(update => update.Action("EditingPopup_Create", "Pengguna"))
                    .Read(read => read.Action("EditingPopup_Read", "Pengguna"))
                    .Update(update => update.Action("EditingPopup_Update", "Pengguna"))
                    .Destroy(update => update.Action("EditingPopup_Destroy", "Pengguna"))
        )
)

<script type="text/javascript">
    function error_handler(e) {
        if (e.errors) {
            var message = "Errors:\n";
            $.each(e.errors, function (key, value) {
                if ('errors' in value) {
                    $.each(value.errors, function () {
                        message += this + "\n";
                    });
                }
            });
            alert(message);
        }
    }
</script>

<div class="form-group">
    @if (ViewBag.Error == null)
    {
        <div class="alert alert-info">@ViewBag.Pesan</div>
    }
    else
    {
        <div class="alert alert-warning">
            <ul>
                @foreach (var err in ViewBag.Error)
                {
                    <li>@err</li>
                }
            </ul>
        </div>
    }
</div>

